server:
  port: 8081

topic:
  name: t.emprunt.order
  
spring:

  datasource:
    url: jdbc:h2:mem:empruntdb
    username: sa
    password:


  h2:
    console:
      enabled: true
      path: /h2-console